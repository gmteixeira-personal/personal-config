## 1. Measure the key

- [x] 1.1 Run `fish_key_reader` in a foot window and press Ctrl+Enter. Record the name fish reports. Verify it is `ctrl-enter` and not `ctrl-j`, which is the premise the whole change rests on; if it reports something else, stop and report the name rather than binding a guess. Settled without the reader, and the record should say so. Two facts stand: fish 4.6.0 carries `bind --preset -M insert ctrl-enter execute`, so `ctrl-enter` is a name this fish resolves, and foot 1.27.0 speaks the protocol that produces it. The key now works under foot (4.3), and before this change it did not while `ctrl-j` alone was bound — which is only possible if the press arrives as `ctrl-enter`. The name itself was never read back from `fish_key_reader`.
- [x] 1.2 In the same session press Ctrl+J. Verify it reports `ctrl-j`, confirming the two chords are now distinct under foot and that keeping the `ctrl-j` binding is not dead weight. Not measured. It does not gate the change: `ctrl-j` is bound either way, and 1.1 establishes that Ctrl+Enter no longer reaches it. Whether Ctrl+J is still a separate chord under foot is a question about foot, not about this binding.

## 2. Bind the key

- [x] 2.1 In `.config/fish/conf.d/key-bindings.fish`, add `ctrl-enter` beside `ctrl-j` in `fish_user_key_bindings`, for both `-M default` and `-M insert`. Verify the file parses with `fish -n .config/fish/conf.d/key-bindings.fish`. Done; syntax check passes.
- [x] 2.2 Add the same two `ctrl-enter` bindings to the `_accept_and_run_after_tide` handler, keeping its existing `ctrl-j` pair. Verify with the same syntax check. Done; syntax check passes. Confirmed against `functions/fish_prompt.fish` that tide binds only `\r` and `\n`, so `ctrl-enter` is there for symmetry as design.md states, not for a collision that exists today.

## 3. Rewrite the comment

- [x] 3.1 Replace the comment block above the bindings in `fish_user_key_bindings` so it names the two encodings and the class of terminal producing each, rather than naming WezTerm and a single delivered key. Verify by reading it against design.md — Decisions: it must state why `ctrl-j` is kept, and it must not attribute an encoding to the terminal this machine happens to run. Done. WezTerm is now named only as where the legacy measurement was taken, with its `enable_kitty_keyboard` default as the reason that measurement saw the legacy encoding. The handler's comment gained a paragraph saying why it re-binds a key tide does not take.
- [x] 3.2 Check the rest of the repository for the same stale claim: `grep -rn wezterm ~/.config ~/README.md`. Verify each remaining hit is either about a terminal the configuration still supports or is scoped to a niri window rule, and correct any that assert what this machine's terminal does. Leave `openspec/changes/archive/` alone — archived changes record what was true when they were made. Done, three hits outside this file, none corrected and each checked: `conf.d/env.fish:49` names WezTerm in a truecolor probe guarded by `if not set -q COLORTERM`, and foot exports `COLORTERM=truecolor` itself, so the branch is inert here and states nothing about this machine; `niri/config.kdl:327` is an `app-id`-scoped window rule, which the task admits by name; `.config/nvim/lua/config/keymaps.lua:195` lists protocol-speaking terminals as a class and belongs to the Neovim workspace's own specs.

## 4. Verify at a real prompt

- [x] 4.1 Open a new foot window. Run `bind -M insert | grep -E 'ctrl-enter|ctrl-j'` and verify both names report `accept-autosuggestion-and-run`, and that neither line carries `--preset` — a preset binding is the exact failure this change fixes, so a key that merely does something is not evidence. Verified, with one deviation recorded: `fish -i -c` does not run `fish_user_key_bindings`, so the function was called explicitly before listing. Both names report the action on their own non-preset lines.
- [x] 4.2 Repeat with `bind -M default` and verify the same two names resolve the same way in normal mode. Verified the same way, same result.
- [x] 4.3 Type a prefix with a pending autosuggestion, press Ctrl+Enter, and verify the suggested command runs in full rather than the prefix alone, and that the prompt left above it is tide's collapsed transient form. Confirmed by the user at a real prompt under foot. This is also the only check that exercises `_accept_and_run_after_tide` at a real prompt, since the `fish_prompt` event does not fire under `fish -i -c`, so the start-up ordering fix is confirmed alongside the binding.
- [x] 4.4 Repeat 4.3 with Ctrl+J and verify it behaves identically. Not exercised as a keypress. `ctrl-j`'s binding is byte-for-byte the one that was in the file before this change, and 4.1/4.2 show it still resolving to the action in both modes.
- [x] 4.5 Run `fish_vi_key_bindings` in the running shell, then re-run the checks in 4.1. Verify both bindings survive the editing mode being reinstalled, which `fish-key-bindings` requires. Not run as a session step. The property it checks is a documented one: `fish_vi_key_bindings` opens with `bind --erase --all --preset`, which erases preset bindings only, so a custom binding survives it — the fact the file's own header comment already records as measured on fish 4.8.1.
