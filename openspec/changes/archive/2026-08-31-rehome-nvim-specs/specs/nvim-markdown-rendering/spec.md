## REMOVED Requirements

### Requirement: Markdown markup is drawn as formatted text

**Reason**: The capability describes the Neovim configuration, which has its own OpenSpec workspace; it was written here only because the change was proposed from the repository root.

**Migration**: The requirement is carried over unchanged as part of `markdown-rendering` in the Neovim workspace, at `.config/nvim/openspec/specs/markdown-rendering/spec.md`. Nothing about Neovim's behaviour changes.

### Requirement: The cursor's own line shows raw markup

**Reason**: The capability describes the Neovim configuration, which has its own OpenSpec workspace; it was written here only because the change was proposed from the repository root.

**Migration**: The requirement is carried over unchanged as part of `markdown-rendering` in the Neovim workspace, at `.config/nvim/openspec/specs/markdown-rendering/spec.md`. Nothing about Neovim's behaviour changes.

### Requirement: Fenced code is highlighted as its declared language

**Reason**: The capability describes the Neovim configuration, which has its own OpenSpec workspace; it was written here only because the change was proposed from the repository root.

**Migration**: The requirement is carried over unchanged as part of `markdown-rendering` in the Neovim workspace, at `.config/nvim/openspec/specs/markdown-rendering/spec.md`. Nothing about Neovim's behaviour changes.

### Requirement: Only markdown buffers are affected

**Reason**: The capability describes the Neovim configuration, which has its own OpenSpec workspace; it was written here only because the change was proposed from the repository root.

**Migration**: The requirement is carried over unchanged as part of `markdown-rendering` in the Neovim workspace, at `.config/nvim/openspec/specs/markdown-rendering/spec.md`. Nothing about Neovim's behaviour changes.

### Requirement: The capability runs on what Neovim bundles

**Reason**: The capability describes the Neovim configuration, which has its own OpenSpec workspace; it was written here only because the change was proposed from the repository root.

**Migration**: The requirement is carried over unchanged as part of `markdown-rendering` in the Neovim workspace, at `.config/nvim/openspec/specs/markdown-rendering/spec.md`. Nothing about Neovim's behaviour changes.

### Requirement: The configuration is tracked

**Reason**: The capability describes the Neovim configuration, which has its own OpenSpec workspace; it was written here only because the change was proposed from the repository root.

**Migration**: The requirement is carried over unchanged as part of `markdown-rendering` in the Neovim workspace, at `.config/nvim/openspec/specs/markdown-rendering/spec.md`. Nothing about Neovim's behaviour changes.
