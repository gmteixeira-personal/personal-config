## REMOVED Requirements

### Requirement: The cursor stays vertically centered

**Reason**: The capability describes the Neovim configuration, which has its own OpenSpec workspace; it was written here only because the change was proposed from the repository root.

**Migration**: The requirement is carried over unchanged as part of `scrolling` in the Neovim workspace, at `.config/nvim/openspec/specs/scrolling/spec.md`. Nothing about Neovim's behaviour changes.

### Requirement: Buffer ends are exempt

**Reason**: The capability describes the Neovim configuration, which has its own OpenSpec workspace; it was written here only because the change was proposed from the repository root.

**Migration**: The requirement is carried over unchanged as part of `scrolling` in the Neovim workspace, at `.config/nvim/openspec/specs/scrolling/spec.md`. Nothing about Neovim's behaviour changes.

### Requirement: The setting survives a restore

**Reason**: The capability describes the Neovim configuration, which has its own OpenSpec workspace; it was written here only because the change was proposed from the repository root.

**Migration**: The requirement is carried over unchanged as part of `scrolling` in the Neovim workspace, at `.config/nvim/openspec/specs/scrolling/spec.md`. Nothing about Neovim's behaviour changes.

### Requirement: Manual top and bottom placement is superseded

**Reason**: The capability describes the Neovim configuration, which has its own OpenSpec workspace; it was written here only because the change was proposed from the repository root.

**Migration**: The requirement is carried over unchanged as part of `scrolling` in the Neovim workspace, at `.config/nvim/openspec/specs/scrolling/spec.md`. Nothing about Neovim's behaviour changes.
